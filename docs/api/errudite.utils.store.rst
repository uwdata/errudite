errudite.utils.store
====================

``Store`` is a storage class that wraps the same types of objects.
It provides functions that assist imports from and exports to ``.json``
files, and functions that make the Store class work just like a ``dict``
object.

.. automodule:: errudite.utils.store
   :members:
